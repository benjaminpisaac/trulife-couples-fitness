# Nixpacks configuration for .NET 8
providers = ["dotnet"]

[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["cd TruLife.API && dotnet publish -c Release -o out"]

[phases.start]
cmd = "cd TruLife.API && dotnet out/TruLife.API.dll"
