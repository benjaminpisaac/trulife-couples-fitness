# Nixpacks configuration for .NET 8
[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["cd TruLife.API && dotnet restore"]

[phases.build]
cmds = ["cd TruLife.API && dotnet publish -c Release -o out"]

[phases.start]
cmd = "cd TruLife.API/out && dotnet TruLife.API.dll"
