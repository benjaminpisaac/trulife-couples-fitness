# Nixpacks configuration for .NET 8
[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish -c Release -o out --self-contained true -r linux-x64"]

[phases.start]
cmd = "./out/TruLife.API"
